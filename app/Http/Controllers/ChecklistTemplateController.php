<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class ChecklistTemplateController
{
    //
    public function index() {
        return ChecklistTemplate::all();
    }

    public function store(Request \) {
        \ = \->validate([]);
        return ChecklistTemplate::create(\);
    }

    public function show(\) {
        return ChecklistTemplate::findOrFail(\);
    }

    public function update(Request \, \) {
        \ = ChecklistTemplate::findOrFail(\);
        \->update(\->validate([]));
        return \;
    }

    public function destroy(\) {
        ChecklistTemplate::findOrFail(\)->delete();
        return response()->noContent();
    }
    public function index() {
        return ChecklistTemplate::all();
    }

    public function store(Request \) {
        \ = \->validate([]);
        return ChecklistTemplate::create(\);
    }

    public function show(\) {
        return ChecklistTemplate::findOrFail(\);
    }

    public function update(Request \, \) {
        \ = ChecklistTemplate::findOrFail(\);
        \->update(\->validate([]));
        return \;
    }

    public function destroy(\) {
        ChecklistTemplate::findOrFail(\)->delete();
        return response()->noContent();
    }
    public function index() {
        return ChecklistTemplate::all();
    }

    public function store(Request \) {
        \ = \->validate([]);
        return ChecklistTemplate::create(\);
    }

    public function show(\) {
        return ChecklistTemplate::findOrFail(\);
    }

    public function update(Request \, \) {
        \ = ChecklistTemplate::findOrFail(\);
        \->update(\->validate([]));
        return \;
    }

    public function destroy(\) {
        ChecklistTemplate::findOrFail(\)->delete();
        return response()->noContent();
    }
    public function index() {
        return ChecklistTemplate::all();
    }

    public function store(Request \) {
        \ = \->validate([]);
        return ChecklistTemplate::create(\);
    }

    public function show(\) {
        return ChecklistTemplate::findOrFail(\);
    }

    public function update(Request \, \) {
        \ = ChecklistTemplate::findOrFail(\);
        \->update(\->validate([]));
        return \;
    }

    public function destroy(\) {
        ChecklistTemplate::findOrFail(\)->delete();
        return response()->noContent();
    }
    public function index() {
        return ChecklistTemplate::all();
    }

    public function store(Request \) {
        \ = \->validate([]);
        return ChecklistTemplate::create(\);
    }

    public function show(\) {
        return ChecklistTemplate::findOrFail(\);
    }

    public function update(Request \, \) {
        \ = ChecklistTemplate::findOrFail(\);
        \->update(\->validate([]));
        return \;
    }

    public function destroy(\) {
        ChecklistTemplate::findOrFail(\)->delete();
        return response()->noContent();
    }
}